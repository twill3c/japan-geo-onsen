# SPEC.md — japan-geo-onsen(日本列島 自然環境・温泉GIS)

<!-- scaffold template v1.30.0 から展開(2026-09-07)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

日本列島の**地形・地質・火山・水・温泉**を一枚の Web 地図に重ね、
それらの関係を「公開データの範囲で」実測して示す。教育・研究・技術実験用。

設計書(`日本列島 自然環境・温泉GIS - AI分析アプリ 設計書.md`)の §1 に従い、
科学的・行政的な温泉探査システムではない。§36 の免責を全画面に常設する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | MapLibre GL JS で日本列島の地図を表示する | must |
| F-02 | 国土地理院タイル(標準・淡色・陰影起伏・色別標高)をレイヤーとして重ねる | must |
| F-03 | レイヤーの表示/非表示・不透明度を UI から変更できる | must |
| F-04 | 標高タイル(DEM)からブラウザ内で等高線を生成し、間隔を UI から変更できる | must |
| F-05 | 産総研 20万分の1日本シームレス地質図V2 をタイルで重ね、クリック地点の凡例を表示する | must |
| F-06 | 気象庁の火山一覧を点レイヤーとして表示し、クリックで属性を表示する | must |
| F-07 | 温泉を**出所ごとに分けた**点レイヤーとして表示し、クリックで属性を表示する | must |
| F-08 | 温泉ごとに 標高・傾斜・地質・最寄り火山距離 を事前計算して保持し、詳細画面に出す | must |
| F-09 | 河川・湖沼レイヤーを表示し、温泉から最寄りの河川・湖沼までの距離を持つ | should |
| F-10 | 温泉分布と地理環境の**実測クロス集計**(地質・標高・傾斜・火山距離・水・植生自然度)を図で出す | must |
| F-11 | 全データについて 出典・ライセンス・取得日・加工方法 を画面から辿れる | must |
| F-12 | 都道府県別の温泉統計(源泉総数・温度別源泉数・湧出量)を表と図で出す | should |
| F-13 | 地図に線を引くと、その区間の**標高断面**を標高タイルから作って図に出す(設計書 §56) | should |
| F-14 | Wikidata が**入浴施設**として持つ場所を第三の点レイヤーにする(温泉の 2 層とは混ぜない) | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Vercel Functions を使わない(静的出力のみ)。cron ゼロ・DB ゼロ | `next.config.mjs` の `output: 'export'` と `out/` に API ルートが無いこと |
| N-02 | 同梱データは合計 20 MB 以下 | `data/` と `public/` の実測バイト数 |
| N-03 | 外部タイルが落ちてもアプリ本体は表示される(設計書 §65/§66) | タイル URL を不達に差し替えた実ブラウザ検品 |
| N-04 | ETL は手動実行。実行のたびに `data/manifest.json` の取得日・件数が更新される | manifest の検査テスト |

## 4. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | **架空データを出荷しない。** `data/` の全フィーチャが raw の実データに 1:1 で遡れる | 件数と ID 集合の一致テスト |
| G-02 | 標高タイル(dem_png)のデコードが Python と TypeScript で完全一致する | 実タイル 8 枚・全 65,536 画素で差 0 |
| G-03 | 温泉点の属性のうち、公開データに無い項目(泉質・泉温・湧出量)は **null のまま**出荷し、画面では「公開データに無い」と明示する | 値が入っていないことのテスト + 文言テスト |
| G-04 | 火山件数は気象庁 JSON の件数と一致し、気象庁公称の活火山数 111 との差は画面と文書で説明される | 件数テスト + 文言テスト |
| G-05 | 温泉点の被覆の穴(0 件の都道府県)を数え、画面に明示する | 実測件数テスト |
| G-06 | 等高線が閉じている/破綻しないこと(marching squares の格子単位の整合) | 合成 DEM での解析解一致 |
| G-07 | 日本語本文にキリル文字等の同形異字が混入していない | `harness/text_hygiene.py` |
| G-08 | 距離計算(温泉→火山)が独立実装(球面 haversine と測地線)で 0.5% 以内に一致する | 二実装照合 |
| G-09 | 点と線分の距離が、線分を刻んで大円距離の最小を取る方法と 0.5% 以内に一致する | 独立経路のオラクル |
| G-10 | 空間索引(篩つき)の答えが、索引を使わない総当たりと**完全に一致**する | 合成データでの照合 |
| G-11 | 環境省 PDF の読み取りが、表が宣言する等式(源泉総数 = Ａ＋Ｂ、湧出量 合計 = 自噴+動力)を満たす | 全 47 行での検算 |
| G-12 | 出所の違う温泉点が混ざらない。Wikidata 由来の点は `provenance` を持ち、**統計には使われない** | 統計の分母が P12 の件数と一致すること |
| G-13 | 3 次メッシュの座標変換が往復し、点に付けた植生が座標から独立に引き直せる | 往復・外部権威(東京駅 53394611)・全点の再現 |
| G-14 | 地形断面が **TypeScript と Python 参照実装で一致**する(点の位置・画素の添字・標高・欠測) | 実タイル 2 本の断面・全 240 点で一致 |
| G-15 | 断面が読む標高が**外部の権威**と桁で食い違わない | 地理院 標高 API との 26 点比較 + 山頂の公表標高 |
| G-16 | 入浴施設の層が温泉の 2 層と**項目単位で重ならず**、統計にも使われない | Q 番号の積集合が空 / 統計の分母が P12 と一致 |
| G-17 | 凡例で意味を分けている色が、**正常視・1 型・2 型のすべて**で見分けられる | 実ファイルから読んだ色の全組で ΔE00 ≥ 10(計器は錨で先に検算) |

## 4-2. 温泉点を二層にしている理由

環境省は温泉地を **2,839** と数えているが、**その一覧は公開されていない**(§6 実測)。
だから 2,839 そのものを点にすることはできない。数える単位も違う ——
環境省の「温泉地」は都道府県が報告した件数、P12 は「観光資源」である。

被覆を広げるために **Wikidata(CC0)の温泉 1,477 件**を第二の層として足した。
重複を除いた地点は 2,455 で、温泉地の 86% にあたる。ただし

- Wikidata は悉皆調査ではなく「記事が書かれた温泉が載っている」データである
- したがって**統計(F-10)には使わない**。対照群は同じ P12 の中から選ぶことで
  「人が登録した場所」という偏りを打ち消しており、出所の違う点を混ぜると揃えが崩れる
- 重なる点は**消さない**。出所が違うので両方残し、重なりの数だけを記録する

## 5. スコープ外

- 未知の温泉の「発見」(設計書 §25)。本アプリは既知分布と環境の関係の可視化に限る
- 泉質・泉温・湧出量の**地点別**表示 — 全国公開データが存在しない(§6 実測)ため
- Deep Learning(設計書 Phase 6)。まず実測クロス集計(設計書 STEP 1)を出す
- リアルタイム火山活動状況(噴火警戒レベル)の表示

## 6. データ源の実測(2026-09-07)

| 対象 | 出典 | 実測 |
|---|---|---|
| 地形・標高・陰影・色別標高 | 国土地理院タイル | std/pale/relief/hillshademap/dem_png/dem5a_png いずれも HTTP 200 |
| 地質 | 産総研 GSJ シームレス地質図V2 Web API 1.3 | タイル `{z}/{y}/{x}`(**y が先**)200。凡例 2,416 件 |
| 火山 | 気象庁 `bosai/volcano/const/volcano_list.json` | **120 件**。気象庁公称の活火山数は **111**(不一致は G-04 で扱う) |
| 温泉(点) | 国土数値情報 P12 観光資源(2014) 種別名称=温泉 または 分類コード 3「温泉・健康」 | 全国 P12a 17,258 点中 **1,320 点**(種別のみ 23 / コードのみ 1,287 / 両方 10)。うち名称に「温泉」「湯」を含むもの 977 点 |
| 温泉(点)の被覆 | 同上 | **10 都府県が 0 件**(福島・東京・富山・福井・愛知・大阪・広島・香川・熊本・宮崎)。分類コード 3 だけで採ると 16 県が 0 件だったが、種別名称=温泉 を足して 10 県になった |
| 温泉の泉質・泉温・湧出量 | — | **地点別の全国公開データは存在しない**。国土数値情報の全 157 データセットに温泉は無く、環境省は都道府県別集計 PDF のみ |
| 河川 | 国土数値情報 河川データ(W05) | 全国 47 県で **286,437 区間・15,459,553 頂点**。ダウンロード 295.9 MB。**利用条件は非商用限定**(他の国土数値情報と異なる) |
| 湖沼 | 国土数値情報 湖沼データ(W09) 2005年版 | **556 面・474,921 頂点**。7.7 MB。商用可 |
| 温泉点の位置の性格 | P12 の住所と行政区域コード | 採録経路 kind の 23 点は **21 点(91%)が住所は市町村どまり**で、点は温泉郷を代表する。住所と行政区域コードの不一致は全 1,320 点中 **1 点**のみ |
| 都道府県別 温泉統計 | 環境省「令和6年度温泉利用状況」PDF(1 頁・47 行) | 全国 **源泉 27,899・湧出量 2,477,713 L/分・温泉地 2,839・宿泊施設 13,425**。温度別は 25度未満 4,074 / 25〜42度 6,863 / 42度以上 12,695 / 水蒸気ガス 1,036 で、**総数の 88.4% しか温度の記録が無い** |
| 同 PDF の読み取り | 同上 | 抽出テキストの行では数字が空白で割れており、割れ方が 3 種類ある(「20,79」+「1」/「4,296」+「,081」/**「4」+「7」= 47**)。3 種類目は書式では決められないので、**文字の座標で切る**(pdfplumber)。全 47 行が例外なく 20 語になる |
| 同 PDF の内部整合 | 同上 | 源泉数の内訳と総数の等式は **47 行中 46 行で成立**。**富山県だけ +1**(内訳 170 / 総数 169)。湧出量は 47 行すべて一致 |
| 地図の点と公表値の対比 | 上記 2 つ | P12 の点 **1,320** に対し温泉地 2,839(**46%**)・源泉 27,899(**5%**)。点が 0 の 10 都府県にも環境省の集計には源泉がある |
| 温泉地 2,839 の一覧 | 環境省 | **公開されていない**。集計 PDF に載るのは数だけで、名前も座標も無い。個々の台帳は都道府県が持つ |
| Wikidata の入浴施設 | Wikidata(CC0) | 入浴施設(Q17521458)系で**温泉(Q177380)に当たらない**もの・日本・座標あり = **108 点**(行 136 を項目に畳んだ数)。うち名前に「温泉」を含むか「湯」で終わるもの **78 点**。内訳は 銭湯 39 / 日帰り入浴施設 24 / スーパー銭湯 9 / 公衆浴場 9 ほか。既存 2 層との場所の重なりは P12 と 14・Wikidata 温泉と 14(消していない)。**発端**: 利用者の指摘で、ほったらかし温泉(Q11277778)が P12 に無く、Wikidata でも上位 33 クラスに温泉が出ないため両層から漏れていた |
| Wikidata の温泉 | Wikidata(CC0) | 座標を持つ日本の温泉 **1,477 件**(問い合わせ結果は 2,338 行で、同一項目の複数座標を畳んで 1,477)。名前が Q 番号のままの項目が 21 件 |
| 二層を重ねた被覆 | 上記 | P12 と 300 m 以内で重なるのは 342 件。**新しい場所 1,135 件**、重複を除いた地点 **2,455**(温泉地 2,839 の **86%**) |
| 植生 | 環境省生物多様性センター 第5回自然環境保全基礎調査 植生3次メッシュ(1992-1996) | 全国 **368,727 メッシュ**(約 1 km)・982 KB(LZH)。群落コード **579 種**はすべて群落表(898 行)で引け、**植生自然度の欠測は 0**。利用条件は **公共データ利用規約 PDL1.0**(出典表示のみ)。自然度の内訳は 植林地 24.8% / 農耕地 21.1% / 二次林 18.6% / 自然林 17.9% ほか |
| 植生の代替候補 | 国土数値情報 L03-b 土地利用細分メッシュ(CC BY 4.0) | 100 m メッシュだが**土地利用であって植生自然度を持たない**。設計書 §8 が求めるのは自然度なので採らない |
| 標高タイルの標高の確からしさ | 国土地理院 標高 API(`getelevation.php`)との比較 | デモ地域の 26 点で **中央値 3.05 m・90 パーセンタイル 9.18 m・最大 10.32 m**。API 側は 1m/5m レーザ測量、タイルは DEM10B 相当なので差は出る。桁の違う食い違いは無い |
| 同 山頂の公表標高との比較 | Wikidata(CC0)の座標と標高 | 周囲 ±25 画素の最大値で 富士山 3774.04 m(公表 3777.24 m・差 -3.2 m)、北岳 3190.98 m(公表 3193.0 m・差 -2.02 m) |
| robots.txt | 各サイト | 制約は env.go.jp の `/cgi-bin/` のみ。他は robots.txt 自体が無い |

## 7. 出荷物(実測 2026-09-08)

| ファイル | 中身 | 件数 | 大きさ |
|---|---|---|---|
| `public/data/volcanoes.geojson` | 気象庁の活火山 | 111 | 26.9 KB |
| `public/data/onsen.geojson` | 温泉点(標高・傾斜・地質・火山/河川/湖沼距離・植生つき) | 1,320 | 1725 KB |
| `public/data/control.geojson` | 対照群(温泉以外の観光地点) | 1,320 | 1,101 KB |
| `public/data/rivers.geojson` | 1 級河川の直轄区間(表示用に間引き) | 11,507 | 2,136 KB |
| `public/data/lakes.geojson` | 湖沼(表示用に間引き) | 547 | 652 KB |
| `public/data/stats.json` | 7 軸のクロス集計と置換検定 | 7 軸 | 8.9 KB |
| `public/data/onsen_stats.json` | 都道府県別の温泉統計(環境省) | 47 | 32.9 KB |
| `public/data/onsen_wikidata.geojson` | 温泉点(Wikidata・CC0) | 1,477 | 1,393 KB |
| `public/data/vegetation_stats.json` | 植生自然度の凡例と全国のメッシュ内訳 | 13 段階 | 4.3 KB |

同梱データ合計 **7.50 MB**(N-02 の上限 20 MB に対して)。

付加の充足率: 温泉は標高 1,319/1,320・地質 1,296/1,320・河川 1,320/1,320・湖沼 1,317/1,320。
対照は標高 1,310/1,320・地質 1,270/1,320・河川 1,320/1,320・湖沼 1,319/1,320。
取れなかった点は集計から外し、外した件数を画面に出す(埋めない)。

距離の二実装照合(G-08)は温泉側の相対差 最大 **0.003364**、対照側 **0.003338**(閾値 0.005)。

河川の表示用の間引きは 424,233 頂点 → **39,814 頂点**(許容 0.0005 度 ≒ 45 m)。
湖沼の面は 556 → 547(頂点が 3 点未満の面と、間引き後に環にならない面を落とした)。
**距離の計算には間引く前のデータを使っている。**

## 8. 温泉と地理環境(実測 2026-09-08)

温泉 1,320 点と、県ごとに同数抽出した対照 1,320 点を 6 軸で比べた。
有意性はラベル置換検定 2,000 回(seed 20260907)の χ²。

| 軸 | χ² | 帰無平均 | p | Cramér の V |
|---|---|---|---|---|
| 最寄りの河川までの距離 | 152.118 | 3.914 | < 0.0005 | **0.2400** |
| 標高 | 69.408 | 3.979 | < 0.0005 | 0.1625 |
| 傾斜 | 57.535 | 4.047 | < 0.0005 | 0.1482 |
| 最寄りの活火山までの距離 | 37.659 | 5.076 | < 0.0005 | 0.1194 |
| 地質(大区分) | 12.506 | 4.015 | 0.01599 | 0.0698 |
| 植生自然度 | 67.550 | 5.034 | < 0.0005 | 0.1632 |
| 最寄りの湖沼までの距離 | 8.567 | 3.938 | **0.06847** | 0.0570 |

**いちばん大きいのは河川との近さ**である。温泉の **42.2%** が河川から 100 m 以内にあり、
対照は **21.8%**(+20.4 pt)。ただしこれは温泉が谷底・川沿いにあることの裏返しでもあり、
地形の効果と切り分けられてはいない。

**湖沼だけは差があるとは言えない**(p = 0.068)。出なかったことも結果として画面に載せる。

**植生自然度は差が中ほどの段階にしか出ない。** 温泉は市街地・農耕地に少なく(−14.2 pt)、
植林地(+7.1 pt)・二次林(+8.5 pt)に多い一方、**自然林・自然草原では +0.09 pt** でほぼ差が無い。
「自然が濃いほど温泉がある」ではない。

V が 0.3 に届く軸はひとつも無く、どの軸でも分布は大きく重なる。
「温泉はこういう場所にある」と言い切れる偏りではない。

一方向でない差も出た。温泉は標高 200〜800 m に寄るが **1200 m 以上では対照のほうが多い**
(−2.0 pt。山頂・高原の観光地が対照に入るため)。火山距離も **100 km 以上の帯では
温泉 25.61% / 対照 25.68% とほとんど同じ**で、差が出るのは中距離の帯だけである。

### 測って捨てた選択肢

- **OpenStreetMap `natural=hot_spring`** — 全国 **238 点**、`temperature` タグ **0 件**。属性が無く点数も少ない
- **OpenStreetMap `amenity=public_bath` / `bath:type=onsen`** — 全国 **5,803 件**で P12 より被覆が広い。
  ただし (a) 源泉ではなく入浴施設である (b) ODbL の継承条件が政府標準利用規約のデータと混ざる。
  被覆の広さと引き換えに意味と licence が濁るため採らない
