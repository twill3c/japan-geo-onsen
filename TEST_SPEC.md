# TEST_SPEC.md — japan-geo-onsen

<!-- scaffold template v1.30.0 から展開(2026-09-07) -->

## 実行規約

- Python: `./.venv/Scripts/python.exe -m pytest -q`（出荷物と raw の突合）
- TypeScript: `npx vitest run`（標高タイルの解読と等高線の幾何）
- 型: `npx tsc --noEmit` / 出荷: `npm run build`
- まとめて: `npm run verify`

**venv を必ずプロジェクトのものにする。** このシェルの `VIRTUAL_ENV` は
別プロジェクト(juchu-desk)を指していることがある。依存を足す前に `sys.prefix` を読む。

## 期待値の出所(HC-016)

| 出所 | このプロジェクトでの実例 |
|---|---|
| 外部権威 | 気象庁「我が国には 111 の活火山があります」(2026-09-07 取得) |
| 外部仕様 | 国土地理院 標高タイル仕様の式 `x = 2^16 R + 2^8 G + B` |
| 実測 | raw の P12 シェープファイルを走査し直して数えた件数・座標 |
| 解析解 | 平面 `z = i` の等高線は `i = level` の鉛直線 |

件数はテストに定数で書かない。**raw から数え直した値と一致すること**を言う。

## オラクルの出所

| フィクスチャ / 対象 | 出所 | 性格 |
|---|---|---|
| `tests-js/fixtures/dem_tiles.json` | 国土地理院の実タイル 8 枚を Python 実装でデコードした SHA-256 | 二実装照合(非循環) |
| `raw/jma/volcano_list.json` | 気象庁の火山一覧 | 件数オラクル(120 = 111 + 3 + 6) |
| `raw/ksj/P12/P12a-14_*.shp` | 国土数値情報の実シェープファイル | 出荷物の全点の遡及先 |
| 合成 DEM(平面・円錐) | 幾何の解析解 | 等高線の正しさ |
| Vincenty 測地線距離 | WGS84 楕円体 | haversine の独立実装対照 |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-01 | G-02 | `decodeDem` が地理院仕様の式どおりに解く | 0/25.6/0.01/−0.01 m と無効値 null |
| T-02 | G-02 | 実タイル 8 枚の全画素が Python 実装と一致 | 524,288 画素の正規化 SHA-256 が一致 |
| T-03 | G-02 | 照合タイルが空振りしていない | 有効画素 > 0・負の標高は含まれない（射程の表明） |
| T-04 | G-06 | 平面 `z = i` の等高線 | `i = level` の鉛直線が 31 本 |
| T-05 | G-06 | 円錐の等高線が閉じる | どの端点も次数ちょうど 2 |
| T-06 | G-06 | 欠測セルに線を引かない | 欠測に接する高さでは 0 本 |
| T-07 | G-06 | 高さの列挙が標高範囲に収まる | 全て `[min, max]` かつ間隔の倍数 |
| T-08 | G-06 | 複数の高さを一度に引いても 1 本ずつと同じ | `contours` と `marchingSquares` が一致 |
| T-09 | G-06 | 全域欠測なら等高線 0 本 | 空配列 |
| T-10 | G-04 | 火山件数が気象庁公表値と一致 | 111・かつ 120 = 111 + 6 + 3 |
| T-11 | G-01 | 火山の全点が名簿の実在項目に遡れる | 番号・名称・座標が一致 |
| T-12 | G-04 | 火口の細分を火山として出さない | 細分 6 件が features に無い |
| T-13 | G-04 | 細分は親火山の属性に残る | `craters` の集合が細分の集合と一致 |
| T-14 | G-01 | 温泉点が raw と同数・取りこぼし無し | (元 ID, 県, 名称) の集合が一致 |
| T-15 | G-01 | 温泉点の座標が raw と一致 | 多重集合として一致 |
| T-16 | F-07 | `onsen_id` が一意 | 重複なし |
| T-17 | G-01 | 重複した元 ID を畳んでいない | raw の重複数と出荷の点数が一致 |
| T-18 | G-03 | 公開データに無い項目が空のまま | 全 1,320 点で 6 項目とも null |
| T-19 | G-05 | 被覆の穴が実測と一致 | 0 件の都道府県の一覧がメタと一致・かつ 0 件でない |
| T-20 | F-01 | 全点が日本の範囲に収まる | 経度 122–154・緯度 20–46 |
| T-21 | G-08 | 火山距離が二実装で一致 | 相対差 < 0.5%(実測 0.34%)・1,000 点超を照合 |
| T-22 | G-08 | 最寄り火山が本当に最寄り | 111 火山への全距離の最小と一致 |
| T-23 | G-07 | 日本語本文に別字種・制御文字が無い | `harness/text_hygiene.py` が 0 |
| T-24 | G-09 | 線分上の点は距離ゼロ | 1e-6 以内 |
| T-25 | G-09 | 端点の外側では端点までの大円距離になる | haversine と一致 |
| T-26 | G-09 | 緯線に垂直な距離が刻み方式のオラクルと一致 | 相対差 0.1% 以内 |
| T-27 | G-09 | 退化した線分は点との距離になる | haversine と一致 |
| T-28 | G-09 | 無作為 120 配置でオラクルと一致 | 最大相対差 0.5% 未満 |
| T-29 | G-09 | 折れ線では全区間の最小が返る | 区間ごとの最小と一致 |
| T-30 | G-09 | 空の折れ線は距離を返さない | None |
| T-31 | F-09 | 間引きは端点を動かさない | 先頭・末尾が同一 |
| T-32 | F-09 | 間引きの誤差が許容値を超えない | 元の全頂点から許容以内 |
| T-33 | F-09 | 間引きは頂点を減らす(対照) | 直線 200 点 → 2 点 |
| T-34 | G-10 | 空間索引の答えが総当たりと完全一致 | 150 点で相対差 1e-9 以内 |
| T-35 | G-10 | 線分が無ければ欠測を返す | (None, None) |
| T-36 | G-10 | 探索範囲の外は欠測になる(埋めない) | (None, None) |
| T-37 | G-10 | 名前は最短の線分のものが返る | 近い側の名前 |
| T-38 | G-11 | 数の書式判定(切ったあとの検算に使う) | 「47」「13,579,988」は通し「20,79」は通さない |
| T-39 | G-11 | 都道府県が 47 行ある | 先頭 北海道・末尾 沖縄県・重複なし |
| T-40 | G-11 | 源泉数の内訳と総数の食い違いは富山県の 1 件だけ | `[("富山県", 1)]` |
| T-41 | G-11 | 湧出量の内訳が合計と一致する | 全 47 行 |
| T-42 | G-11 | 温度別源泉数は総数と**一致しない** | 被覆 0.6〜0.95(実測 0.884) |
| T-43 | G-11 | 全国合計が公表の桁に収まる | 源泉 2〜4 万・宿泊施設 0.8〜2 万 |
| T-44 | G-11 | 負の値や欠測が無い | 全列 >= 0 |
| T-45 | G-12 | 同じ Wikidata 項目の複数座標を畳んでいる | 行 2,338 → 項目 1,477 |
| T-46 | G-12 | 出荷した点が生データの項目数と一致 | QID 集合が一致 |
| T-47 | G-12 | 全点が `provenance = wikidata` を持つ | 全件 |
| T-48 | G-03 | 公開データに無い項目が空のまま(Wikidata 層も) | 全 1,477 点で 6 項目とも null |
| T-49 | G-12 | 名前が無い項目に名前を作らない | ラベルが Q 番号のものは null(実測 21 件) |
| T-50 | G-12 | 全点が日本の範囲に収まる | bbox 内 |
| T-51 | G-12 | ID が一意 | 重複なし |
| T-52 | G-12 | P12 と重なる点の数が記録されている | 数え直した値とメタが一致 |
| T-53 | G-12 | **統計は P12 だけで取られている** | 統計の分母が P12 の件数と一致・wikidata 由来が 0 |
| T-54 | G-13 | 3 次メッシュ 1 個の大きさ | 緯度 30 秒 × 経度 45 秒 |
| T-55 | G-13 | 1 次メッシュの南西端 | 5339 → 南 53/1.5 度・西 139 度 |
| T-56 | G-13 | 東京駅が 53394611(外部権威) | 一致 |
| T-57 | G-13 | コード → 中心 → コード が恒等 | 桁の組み合わせを網羅した合成コードで一致 |
| T-58 | G-13 | 隣り合うメッシュが接している | 境界がぴたりと合う |
| T-59 | G-13 | 桁が足りないコードは例外 | ValueError |
| T-60 | G-13 | 区画の番号が範囲外なら例外 | 5〜6 桁目 > 7 で ValueError |
| T-61 | G-13 | メッシュ件数が生データと一致 | 368,727 |
| T-62 | G-13 | 群落コードがすべて群落表で引ける | 579 種すべて |
| T-63 | G-13 | 自然度がすべて凡例にある | 欠測 0 |
| T-64 | G-13 | 自然度の内訳がメッシュ総数と一致 | 取りこぼし 0 |
| T-65 | G-13 | 自然度 1〜10 がすべて実在する | 空の段階が無い |
| T-66 | G-13 | 点に付けた植生が座標から引き直せる | 3 層すべてで再現 |
| T-67 | G-13 | メッシュが無い点は埋めない | 欠測が実在し、null になっている |
| T-68 | G-13 | 統計の植生軸が欠測を分母に入れていない | onsen_n が実測と一致 |
| T-69 | G-14 | 距離が球の定義と一致(緯度 1 度 = 111.1950802335329 km) | 手計算と 1e-9 |
| T-70 | G-14 | 大円上の刻みの両端・中点・区間和が全長に戻る | 1e-6 以内 |
| T-71 | G-14 | 画素の位置が `etl/common.lonlat_to_pixel`(別実装)と一致 | 3 段 × 200 点で完全一致 |
| T-72 | G-14 | 1 画素の大きさが赤道の全周から導ける | 1e-12 |
| T-73 | G-14 | 段の選び方が長さに対して単調で、タイル枚数の上限を超えない | 上限 24 枚 |
| T-74 | G-14 | 点の間隔が 1 画素より細かくならない | 上限に当たらない範囲で全件 |
| T-75 | G-14 | 誇張倍率が縦横の縮尺の比になる(起伏 0 でも 0 除算しない) | 手計算と一致 |
| T-76 | G-14 | **実タイルの断面が Python 参照実装と全点一致**(位置・添字・標高・欠測) | 2 本 240 点で差 0 |
| T-77 | G-14 | 欠測を通る線が実在し、その点は null のまま | 有効 63/120 の線で一致 |
| T-78 | G-14 | 平面で刻んだ中点と球面の中点が実際に違う | 1 km 以上 |
| T-79 | G-15 | 断面が読む標高が地理院 標高 API と桁で食い違わない | 26 点・中央値 3.05 m |
| T-80 | G-15 | 山頂の公表標高(Wikidata)と周囲最大値が近い | 富士山 -3.2 m / 北岳 -2.02 m |
| T-81 | F-13 | 地図を 2 回クリックすると断面図が出る(実ブラウザ) | 図・誇張倍率・出典・線の残存 |
| T-82 | F-13 | 累積の上り下りが刻みで変わることを実データで確かめる | 330.5→375.4 m(頭打ちも確認) |
| T-83 | G-16 | 入浴施設の層の点数が生データの項目数と一致 | 行 136 → 項目 108 |
| T-84 | G-16 | 全点が出所(wikidata-facility)と分類を持つ | 分類が空の点は 0 |
| T-85 | G-16 | 公開データに無い項目は null のまま | 6 欄すべて |
| T-86 | G-16 | 温泉の層と**項目単位で重ならない** | Q 番号の積集合が空 |
| T-87 | G-16 | 場所の重なりは消さずに数を記録している | P12 と 14・Wikidata 温泉と 14 |
| T-88 | G-16 | **ほったらかし温泉が入浴施設の層に出て、温泉の 2 層には無い** | Q11277778 |
| T-89 | G-16 | 統計はこの層を使っていない | 分母が P12 と一致 |
| T-90 | G-16 | 銭湯・公衆浴場が実在する(注意書きが空文でない) | 分類の内訳から |
| T-91 | F-14 | 地図で当該地点へ寄ると入浴施設が描かれ、温泉とは限らないと断る(実ブラウザ) | 名前で照合 |
| T-92 | G-17 | 色覚シミュレータが錨を通る(白・灰・黒が動かない / 赤緑が 2 型で縮む) | ΔE00 0.00 / 86.6→19.8 |
| T-93 | G-17 | 恒等変換の偽計器は錨で落ちる(陽性対照) | 空振りしていないこと |
| T-94 | G-17 | 点レイヤーの色が三視型すべてで ΔE00 ≥ 10 | 最小 17.4(onsen×火山警戒・1型) |
| T-95 | G-17 | 温泉と火山が別の色に見える | かつて 2 型で 0.1 だった組 |
| T-96 | G-17 | どの点も地図の下地の上で見える | 全色 × 三視型 |
| T-97 | G-18 | Wikipedia 層の点数が生データと一致 | 記事 1,862 → 座標あり 1,344 |
| T-98 | G-18 | 全点が出所・記事への経路・座標の出どころを持つ | wikipedia / wikidata の別 |
| T-99 | G-18 | 公開データに無い項目は null のまま | 6 欄すべて |
| T-100 | G-18 | **地図と一覧が排他**(同じ記事が両方に出ない) | 積集合が空 |
| T-101 | G-18 | 座標の無い記事が**漏れなく**一覧に載る | 集合が完全一致 |
| T-102 | G-18 | **指摘された温泉が一覧に載る** | Q671405 / Q108702377 |
| T-103 | G-18 | 一覧にまとまり・概念が混ざることを隠していない | 温泉郷 17 件 |
| T-104 | G-18 | 一覧の出所ごとの件数が本文と一致 | 518 / 432 |
| T-105 | F-15 | 層を点けると地図に載り、一覧ページに名前が並ぶ(実ブラウザ) | 900 件以上 |

## 対照(変異体)の実測記録

テストが空振りしていないことを、実装/データを壊して確かめた。

| 変異体 | 実施日 | 落ちたケース | 分かったこと |
|---|---|---|---|
| 負標高の折返しを `2^24` → `2^23` | 2026-09-07 | T-01 のみ | **実タイル照合 8 枚は緑のまま**。手元のタイルは全て陸域で負の標高を 1 画素も含まない。負の分岐を守っているのは仕様式のケースだけである(T-03 に明記) |
| `decodeDem` の R と B を入替 | 2026-09-07 | T-01・T-02(8 枚とも) | 実タイル照合はチャネルの取り違えを捕まえる |
| 出荷データに架空の泉質を 1 件注入 | 2026-09-07 | T-18 | 架空値の混入を捕まえる |
| 出荷データの座標を 1 点だけ 0.01° ずらす | 2026-09-07 | T-15・T-21・T-22 | 座標の改変は raw 照合と距離検算の両方で落ちる |
| 点と線分の距離で楕円体の 1 度長を使う(球の haversine と混在) | 2026-09-08 | T-25・T-26・T-27 | 同じ関数の中で二つの地球が混ざると 0.2% ずれる。**実装を書いた直後にテストが捕まえた**(基準を球へ統一) |

## 速さを変えたときの検算(2026-09-08)

最寄り河川の走査は、はじめ全候補に厳密な距離関数を当てていた。その関数は
垂線の足が線分の外に落ちるとき haversine(三角関数 5 回)を返すので、
全国 286,437 区間に対しては約 12 億回の三角関数計算になり、CPU 51 分でも終わらなかった。

篩(局所平面・三角関数なし)で上位 8 件に絞ってから厳密な関数を当てる二段構えに変えた。
**速くしたぶん、答えが変わっていないことを総当たりで確かめる**のが T-34 である。
索引を使わない経路と 1e-9 以内で一致する。

## 書式で切る方式を捨てた記録(2026-09-08)

環境省 PDF は抽出テキストの行で数字が空白で割れる。はじめ「3 桁区切りとして正しくなる
隣接対だけを繋ぐ」書式ベースの復元を書き、2 種類の割れ方まではそれで通った。

3 種類目で破綻した —— **「4」+「7」(= 47)は、どちらも単体で正しい数**である。
「2 列なのか 1 つの数なのか」は文字の並びからは決められない。

代わりに `pdfplumber` の `extract_words` で**文字の座標**から語を作った。
数の中の細い隙間と列の間の広い隙間は座標で区別できる。実測すると
**全 47 行が例外なく 20 語**になり、推測が要らなくなった。
書式判定は「切る道具」から「切ったあとの検算」に降格した。

## 出所の違う層を混ぜないための検査(2026-09-08)

地図の被覆を広げるために Wikidata(CC0)の温泉 1,477 件を第二の層として足した。
被覆は広がるが、**統計の土台は壊してはならない** —— 対照群は同じ P12 の中から
選ぶことで「人が登録した場所」という偏りを打ち消しているからである。

そこで T-53 で「統計の分母が P12 の件数と一致し、統計に使う点に wikidata 由来が
1 つも無いこと」を固定した。**この検査は、将来うっかり層を統合したときに落ちる。**

## テストの側が間違っていた記録(2026-09-09)

3 次メッシュの往復検査で、合成コードの 5〜6 桁目に「9」を混ぜて書いた。
実際には 5〜6 桁目は 1 次メッシュを **8 等分**する番号なので 0〜7 である
(7〜8 桁目だけが 10 等分で 0〜9)。**実装は正しく、テストが落ちた。**

範囲外を黙って通さない実装にしていたおかげで、テスト側の誤りとして現れた。
通していたら、存在しないメッシュの座標を平然と返していた。

## 固定の待ち時間は計器にならない(2026-09-09)

本番へ配った直後の実ブラウザ検品で、**温泉(国土数値情報)・温泉(Wikidata)・火山**の
3 層が揃って「0 件」で落ちた。同じ検査が手元では緑で、同じ本番 URL に対して
少し後に走らせても緑になる。

実測すると、配信し始め(端の保存が冷えている状態)では点が描かれるまで **約 3.0 秒**、
暖まった後は **約 1.1 秒**だった。検査は `page.evaluate` の中で **2,500ms 固定で待って**
から数えていたので、冷えた一回だけが 0 件になっていた。**実装は正しく、計器が回線の
速さを測っていた。**

固定待ちをやめ、**出るまで待ち、締切(既定 20 秒)を過ぎたらそのときの数で判定する**
形にした。締切超過は 0 件のまま落ちるので、本当に描かれない故障はこれまで通り捕まる。

「待ちを延ばす」修正は、検査を骨抜きにしたのと見分けがつかない。そこで陽性対照を
`harness/_probe_render_timing.mjs` に置いた。

- (1) 3 層が初めて 0 でなくなる時刻を測る
- (2) `page.route` で `onsen.geojson` / `onsen_wikidata.geojson` / `volcanoes.geojson` を
  **空の FeatureCollection** に差し替え、8 秒待っても 3 層とも 0 のままであることを見る

実測: (1) 1,106ms(暖まった本番)、(2) 3 層とも 0 のまま — **検査は空振りしていない**。

## 図の数字は正しく、言葉のほうが強すぎた(2026-09-10)

断面図に「上り・下りの合計は刻みの細かさで変わります／刻みに依らないのは起伏のほうです」
と書いた。**後半が言い過ぎだった。**

実タイル(八ヶ岳 z14・2.723 km・1 画素 7.64 m)で刻みを変えて測ると:

| 刻み | 上りの合計 | 起伏(最大−最小) |
|---|---|---|
| 143.3 m | 330.50 m | 330.50 m |
| 55.6 m | 354.29 m | 354.29 m |
| 27.5 m | 362.58 m | 350.03 m |
| 13.7 m | 366.79 m | 354.29 m |
| 6.8 m(≈1 画素) | 373.05 m | 354.29 m |
| 3.4 m | 375.40 m | 354.29 m |
| 0.9 m | 383.95 m | — |

上りは単調に増えるが、**1 画素より細かく刻んでも大きくは増えない**(同じ画素を
読み直すだけ)。そして**起伏も粗すぎる刻みでは 7% 低く出る** —— 山も谷も飛ばすからである。

さらに出荷側には点数の上限(800)がある。46 km の線では刻みが 58 m になり、
1 画素(15 m)より粗い。**画面の但し書きのほうを実測に合わせて書き直し**、
粗く刻んでいるときはその旨を出すようにした(T-82)。

## 目の検査で見つけたもの(2026-09-10)

断面図は数値の検査を全部通っていたが、撮って見ると 2 つ直すところがあった。

- x 目盛りが「0.00・10・20・30・40」と桁がそろわない。値ごとに桁を決めていたため。
  **間隔で決める**ようにした
- 上端の「標高 m」が viewBox から **0.56 px** はみ出していた(`getBBox()` で実測)。
  文字は切れる寸前だった。基線を 2 px 下げた

撮影と実測は `harness/_shot_profile.mjs`(はみ出しは viewBox と `getBBox()` の差で測る)。

## 利用者の指摘から始まった検査(2026-09-10)

「ほったらかし温泉が地図に無い」という指摘を受けて調べた。**二つの出典それぞれで
別の理由**だった。

- 観光資源データ(P12, 2014): 全国 17,258 点に該当なし。抽出規則で落としたのではなく
  **台帳に無い**(山梨市の登録は 11 件、うち分類コード 3 は 4 件)
- Wikidata: 項目は在る(Q11277778・座標・国・所在地とも揃う)が `instance of` が
  **日帰り入浴施設**。上位クラスを 33 件たどっても 温泉(Q177380) に当たらないので、
  既存クエリ(`P31/P279* = Q177380`)に掛からない

同じ理由で外れる項目を数えると **108 件**(名前に温泉/湯を持つもの 78 件)。
第三の層として足したうえで、**経緯そのものを検査に落とした**(T-88)。
経緯を文章にだけ書くと、次の変更で静かに消えるからである。

あわせて、画面に「温泉とは限りません」と書いた以上、**そう書くに足る事実がある
こと**も検査する(T-90: 銭湯 39・公衆浴場 9 が実在する)。注意書きだけが残って
中身が変わっている状態を作らない。

## 色の衝突は、幾何の検査でも目視でも捕まらない(2026-09-10)

第三の層の色を決めるために色差を測ったら、**既存の 2 層の間に欠陥**が出た。

| 組 | 正常視 | 1 型 | 2 型 |
|---|---|---|---|
| 温泉 #c2410c × 火山 #b45309 | 6.9 | 3.2 | **0.1** |

溢れも重なりも無く、値としては違う色で、正常視では(かろうじて)見分けられる。
**色覚型を変えて色差を計算しない限り出てこない**故障である。

火山を #1f2937(通常)・#7f1d1d(警戒)に替え、全組 × 三視型の最小 ΔE00 は
**0.1 → 17.4** になった。

### 計器のほうが危なかった

最初 Hunt-Pointer-Estevez の LMS 行列に Viénot 1999 の係数を当てて測り、
「Wikidata の層と地図の下地が 1 型で ΔE 3.7」という**偽の欠陥**を出しかけた。
行列と係数の組が違い、**白が白のまま残らない**計器だった(protan で L' が負になり、
クリップされて水色になる)。

色覚シミュレータは壊れていても「それらしい色」を返すので、絵として成立してしまう。
そこで錨を 4 本置き、**テストの中で先に計器を検算してから**本題を判定する(T-92)。
さらに、その検算が空振りしていないことを恒等変換の偽計器で確かめる(T-93)。→ HC-257

## 「無い」と「入口が違う」を取り違えかけた(2026-09-10)

Wikipedia から温泉記事を辿るのに、まず `Category:日本の温泉` を叩いて **0 件**を得た。
危うくそのまま「Wikipedia のカテゴリからは辿れない」と読むところだった。

**そのカテゴリは存在しない。** 実体は都道府県別(`Category:山梨県の温泉` など)である。
入口の実在を確かめる `exists()` を先に通す形に変え、存在しない入口があればその数と名前を
出すようにした。0 件は「無い」の証拠にならない —— **入口が在ることを確かめてから数える。**

続けて深さ 2 まで辿ったところ、今度は `Template:Arima onsen` や「こおろぎ橋」「よみうりランド」
まで混ざった。名前空間 0 に絞り、直下だけを採る形に直した。**測る向きを変えるたびに
違うものが入ってくる**ので、両方の数を記録してある(深さ2: 記事 2,191 / 深さ0: 1,862)。

## 一覧に載せる数の意味を測った(2026-09-10)

「座標が無くて地図に出せない温泉 950 件」と書きかけたが、内訳を数えると
**温泉郷 17・「日本三古湯」のような概念・閉業した施設**が同じ分類で入っていた。
`432 件の温泉が出せない`とは言えない。分類を各項目に添え、内訳の表を画面に出し、
検査(T-103)で「まとまりが実在すること」を押さえた。
**注意書きだけが残って中身が変わる**ことを防ぐためである。

## 既定で消えている層を、起動時に読んでいた(2026-09-10)

第四の層を既定 OFF で足したのに、**本番の起動時に 1.8 MB を取りに行っていた。**
`MapView` が `ONSEN_LAYERS` を起動時にまとめて `addSource` していたためで、
`defaultVisible: false` は**描かないだけ**でデータは配られる。

同じ誤りは loop_002 で水の層(2.85 MB)について一度直している。
規約は「点けたときに初めて足す」で、点の層でそれを踏み直した。

見つけたのは本番検品の `_which3.mjs` が出す「起動時に取得したデータ」の一覧である。
検品 57 件はすべて緑だった —— **画面は正しく、配る量だけが違う**故障は、
表示の検査では捕まらない。

`harness/_probe_startup.mjs` を置いて、起動時に取るファイルとその合計を実測し、
既定で消えている層(rivers / lakes / onsen_wikipedia)が入っていないことを見る。
実測 **5.3 MB → 3.51 MB**。

## 未実装のゲート

- **G-06 の「閉じている」は合成 DEM でのみ検算している。** 実タイルでは、
  タイル境界・欠測・格子の間引きによって開いた線が正常に出る。
  実データに対する閉包の検査は置いていない
